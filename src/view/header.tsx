import React from 'react'

// onClick={() => setTypes(!isTypes)}

const Header = () => {
    return (
        <div className="header">
            <button>
                Toggle Types
            </button>
        </div>
    )
}

export default Header